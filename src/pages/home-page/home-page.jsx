import React from 'react';
import './home-page.scss'

const cb = 'home'

export default function HomePage() {
    return (
        <div className={cb}>
            Long home page! (Footer is below the fold)
        </div>
    )
}